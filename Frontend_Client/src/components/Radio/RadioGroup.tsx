import type { FC, PropsWithChildren } from 'react';

interface IRadioGroup extends PropsWithChildren {}
const RadioGroup: FC<IRadioGroup> = ({ children }) => {
    return <div></div>;
};
export default RadioGroup;
